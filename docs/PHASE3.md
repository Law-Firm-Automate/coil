# Phase 3: the sizeable Clio gaps

Read docs/CONVENTIONS.md first (all of it). Same hard rules: models.py is mine (schema below is already in place when you start), no base.html/helpers.py/__init__.py/seed.py edits except where this doc says "additive", own only the files in your list, tests own their DB, run the whole suite, do not commit, no dev server.

## Schema already added (read models.py again)
- `Holiday(date, name)`; `CourtRuleSet(name, jurisdiction, description, is_active)`; `CourtRule(ruleset_id, trigger, title, offset_days, day_type calendar|court, direction after|before, roll forward flag, kind deadline|court_date|task, notes, sort)`; `Task.rule_id`, `Task.trigger_date`, `Task.rule_trigger`.
- `DocTemplate(name, kind docx|html, practice_area, description, path, body_html, fields_json, is_active)`; `Document.template_id`, `Document.version`, `Document.version_of_id` (root document id), `Document.is_current`, `Document.folder`, `Document.tags`.
- `Message.subject`, `Message.message_id`, `Message.has_attachments`.
- `Account(code, name, type income|expense|asset|liability|equity, is_active, is_system)`; `LedgerEntry(date, account_id, amount_cents signed, description, payee, reference, matter_id, payment_id, expense_id, source manual|payment|expense|import, cleared, cleared_on)`; `BankImport(filename, account_id, rows, imported_at)`; `OperatingReconciliation(period_end, statement_balance_cents, book_balance_cents, outstanding_json, balanced, notes)`.
- `AiRun(kind, entity, entity_id, prompt_chars, output_chars, model, cost_cents, user_id, created_at)`; `Firm.ai_enabled`.
- `IntakeLead.stage`, `value_cents`, `next_follow_up_on`, `assigned_user_id`, `lost_reason`; `FollowUpSequence(name, steps_json, is_active)`; `LeadSequence(lead_id, sequence_id, started_on, next_step, status)`; `Firm.sequences_auto_send`.
- `ApiToken(user_id, name, token_hash, prefix, scopes, last_used_at, revoked_at)`; `Webhook(url, events, secret, is_active)`; `WebhookDelivery(webhook_id, event, payload_json, status, attempts, response_code, last_at)`.

## Agent E: court rules + document automation
Owns: new `app/blueprints/rules.py`, `app/blueprints/doctemplates.py`, templates `rules/`, `doctemplates/`, `app/blueprints/tasks.py` (only to add the "from rules" section and rule columns display), `tasks/` templates. Deps: `docxtpl` is installed.
- Rules library `/settings/rules` (CRUD rule sets and rules; import/export a rule set as JSON), holidays `/settings/holidays` (CRUD + "load US federal holidays for year" button computed in code, no network).
- Day math in `app/blueprints/rules.py`: `compute_deadline(trigger_date, rule, holidays)`; court days skip weekends and holidays; calendar days count all and roll forward to the next court day when the result lands on a weekend/holiday and the rule's roll flag is on; `direction=before` counts backwards and rolls backwards. Unit-test it hard.
- Ship two starter rule sets created lazily when none exist: "Federal civil (FRCP), generic" (answer 21 days after service; Rule 26(f) conference 21 days before scheduling conference; initial disclosures 14 days after 26(f); discovery responses 30 days after service; reply brief 14 days after response; notice of appeal 30 days after judgment) and "Texas civil (TRCP), generic" (answer: 10:00 a.m. on the Monday next after 20 days from service; discovery responses 30 days; motion for new trial 30 days after judgment; appeal 30 days after judgment). Every rule carries a `notes` sentence saying it is generic and must be checked against the local rules. Do not claim the sets are complete or authoritative anywhere in the UI.
- `/matters/<id>/rules/apply` (GET picks rule set + trigger + trigger date; POST creates one Task per matching rule with due_on computed, kind from the rule, `rule_id`, `trigger_date`, `rule_trigger`; skips tasks that already exist for the same rule + trigger date). Put the route in rules.py (url `/rules/matters/<id>/apply`) and link it from tasks/index.html and from a "Court deadlines" button you add in `matters/detail.html` tasks tab (small targeted edit, re-read first).
- Document templates `/doctemplates` (list, upload .docx or write HTML, practice area, detect merge fields in the file and list them), `/doctemplates/<id>/generate?matter_id=` (GET shows the fields with values prefilled from firm/office/client/matter/custom fields/responsible/today plus blanks for unknown fields; POST renders: docx via docxtpl, html via a fresh jinja2 Environment then to PDF with app.services.pdf; saves the output as a Document on the matter with `template_id`, name `<template> - <matter number>.<ext>`; audit). Merge field names: firm_name, firm_address, firm_phone, firm_email, office_address, attorney_name, attorney_email, today, client_name, client_first_name, client_last_name, client_address, client_email, client_phone, matter_name, matter_number, practice_area, court, case_number, opposing parties (adverse_parties joined), plus every matter custom field as `cf_<snake_name>` and contact custom fields as `ccf_<snake_name>`. Ship one sample HTML template (a generic representation-closing letter) lazily.
- "Generate document" link on the matter Documents tab (targeted edit in matters/detail.html).

## Agent F: document management + email filing + PWA
Owns: `app/blueprints/documents.py`, `documents/` templates, new `app/blueprints/emailin.py`, `app/cli.py` (additive: `emailin` command), `app/static/manifest.webmanifest`, `app/static/sw.js`, `app/static/icons/*` (generate simple PNG icons with Pillow if available, else a plain SVG), and ONE additive line block in `app/templates/base.html` head and in `public.html` head for the manifest + theme-color + service worker registration (this is the exception to the base.html rule; keep it to the head).
- Versions: `/documents/<id>/versions` (list), `/documents/<id>/new-version` (POST file; new Document row with `version_of_id` = root, `version` = max+1, previous `is_current` False, same folder/tags/shared flag; audit). Lists and matter tabs show only current versions with a "v3" badge and link to history. Download of an old version allowed.
- Folders and tags: `folder` free-text path like "Pleadings/Motions"; documents list groups by folder with a folder filter; `tags` comma list with filter; bulk move/tag form on the list (checkbox rows).
- Full-text search `/documents/search?q=` across name, tags, folder, `extracted_text` (SQL LIKE on normalised text is fine; show a snippet around the first hit); a search box on the documents page and in the matter Documents tab.
- Email filing `python -m app.cli emailin`: IMAP (env `IMAP_HOST`, `IMAP_PORT`, `IMAP_USER`, `IMAP_PASS`, `IMAP_FOLDER` default INBOX; add them to `.env.example` and to `Config` in app/config.py — config.py is additive-allowed for you). Fetch unseen messages; match a matter by `[M-1002]` style number anywhere in subject/body or by sender email to a client with exactly one open matter; store as Message channel=email direction=in with subject, message_id (dedupe), body text; save attachments as Documents on the matter in folder "Email"; mark seen; unmatched messages get filed to a review list `/messages/unfiled` (Message with no contact/matter) where staff pick a matter. Show on `/settings/integrations` the BCC/forwarding instruction ("forward or BCC <IMAP_USER> and put the matter number in brackets in the subject"). Idempotent on message_id. Test with a fake IMAP by monkeypatching the fetch function.
- PWA: manifest (name Coil, short_name Coil, standalone, theme colour from CSS, icons), a service worker that caches the app shell (static assets) and serves a small offline page for navigation failures; register it in base.html and public.html. Do not cache API/POST responses.

## Agent G: operating accounting + API + webhooks + Chrome extension
Owns: new `app/blueprints/accounting.py`, `app/blueprints/api.py`, `app/blueprints/webhooks_out.py`, templates `accounting/`, `settings/api.html`, `settings/webhooks.html`, `app/blueprints/settings.py` (only to add routes/links for API tokens and webhooks; re-read first), `app/cli.py` (additive: `webhooks` retry command), new top-level `extension/` directory, and `app/helpers.py` additive only (`emit_event(name, payload)`).
- Chart of accounts: seed lazily when empty (Operating Bank 1000 asset, Client Trust Liability 2100 liability (informational), Legal Fees 4000 income, Reimbursed Expenses 4100 income, Interest Income 4200, Card Surcharge Income 4300, Merchant Fees 6100 expense, Rent 6200, Software 6300, Salaries 6400, Marketing 6500, Office 6600, Client Costs Advanced 1300 asset, Owner Draw 3100 equity, Owner Contribution 3000 equity). `/accounting/accounts` CRUD (system accounts cannot be deleted).
- Ledger `/accounting` (entries by month with account filter, running bank balance), `/accounting/new` (manual entry: date, account, amount, payee, description, matter optional, reference), edit/delete for manual entries only.
- Automatic entries: a `post_payment(payment)` hook that writes Legal Fees / Reimbursed Expenses / Surcharge / Merchant Fees entries when a Payment with account=operating is recorded (call it from a SQLAlchemy after_insert listener on Payment so you do not edit payments.py), and `post_expense(expense)` for non-billable expenses (after_insert on Expense) to Client Costs Advanced when billable else Office. Entries carry payment_id/expense_id and are deleted if the source is deleted (after_delete listeners).
- Bank CSV import `/accounting/import` (upload; detect Date/Description/Amount or Debit/Credit columns; preview with auto-match to existing entries by amount and date ±3 days, shown as "matched" (marks cleared) or "new" (creates an entry in a "Needs category" state = account NULL); commit). `/accounting/uncategorised` to assign accounts.
- Operating reconciliation `/accounting/reconcile` (period end + statement balance; book = sum of bank-affecting entries; outstanding = uncleared items; balanced flag; report page) mirroring the trust one.
- Reports `/accounting/pnl?from=&to=` (income vs expense by account, net), `/accounting/balance` (bank per book, trust liability per trust ledger, receivables from invoices), CSV for each; link both from `/reports`.
- REST API `/api/v1/*` with Bearer tokens (`ApiToken`: created at `/settings/api` showing the raw token once, sha256 stored; scopes read|write; revoke). Endpoints: `GET /api/v1/me`, `GET /api/v1/matters?q=&status=`, `GET /api/v1/matters/<id>`, `GET /api/v1/contacts?q=`, `POST /api/v1/time` (matter_id, minutes or hours, description, date, billable), `GET /api/v1/time?matter_id=&from=&to=`, `POST /api/v1/timer/start|stop`, `GET /api/v1/invoices?status=`, `GET /api/v1/tasks?due=today`. JSON errors, 401 on bad token, 403 on scope. Rate limit 120/min per token in memory. `/api/` must be CSRF-exempt: you cannot edit helpers.py's list, so mount tokens under the existing exempt prefix `/webhooks/`? No: use `/api/v1/` and add `"/api/v1/"` to `CSRF_EXEMPT_PREFIXES` in app/helpers.py (additive one-token change is allowed for you). Document the API on `/settings/api`.
- Outgoing webhooks: `Webhook` CRUD at `/settings/webhooks` (url, events checklist, secret); `emit_event(name, payload)` in helpers.py queues a `WebhookDelivery` per active webhook subscribed to the event and attempts delivery immediately (POST JSON with `X-Coil-Signature: sha256=<hmac>` and `X-Coil-Event`, 5 s timeout); `python -m app.cli webhooks` retries failed deliveries up to 5 times with backoff. Emit from SQLAlchemy after_insert/after_update listeners for: matter.created, matter.closed, invoice.sent, invoice.paid, payment.received, engagement.signed, document_signature.signed, intake_lead.created, task.completed. Test with a monkeypatched requests.post.
- Chrome extension `extension/` (Manifest V3): popup with API base URL + token fields (chrome.storage), a matter search box hitting `/api/v1/matters?q=`, a running timer (start/stop via the API), and "log time" (minutes + description). Keep it under 300 lines, no build step. Include `extension/README.md` with load-unpacked instructions. You cannot test it in a browser here; write the API tests instead and keep the JS simple.

## Agent H: AI features + CRM pipeline
Owns: new `app/llm.py` (follow `~/.claude/skills/fleet-llm/SKILL.md` exactly: read it in full first; env-gated on `OPENROUTER_API_KEY` / `ANTHROPIC_API_KEY`, spend cap, template fallback when no key), new `app/blueprints/ai.py`, templates `ai/`, `app/blueprints/intake.py`, `intake/` templates, `app/cli.py` (additive: `sequences` command), `app/config.py` additive for the AI env names, and small targeted "AI" buttons in `matters/detail.html` (overview tab), `invoices/detail.html` (narrative cleanup), `documents/index.html` (extract dates) — re-read each file right before editing.
- `app/llm.py`: `complete(prompt, system="", max_tokens=800, kind="") -> str`; records an `AiRun` per call with cost estimate; raises `LLMUnavailable` when no key or over the daily cap (`AI_DAILY_CAP_CENTS`, default 300); `Firm.ai_enabled` must also be on. Never send more than 12,000 characters of context; truncate with a note.
- Features (each a POST route under `/ai/...` returning to the page with the result shown in a card, plus a "use this" action where it applies):
  1. Invoice narrative cleanup: `/ai/invoice/<id>/polish` rewrites every time line description into clear, client-facing past-tense narratives without changing hours or amounts; shows before/after; "Apply" updates the lines (draft invoices only).
  2. Matter summary: `/ai/matter/<id>/summary` from matter details, notes, recent time entries, messages, tasks; shows a 150-word summary and open items; "Save as note".
  3. Extract dates from a document: `/ai/document/<id>/dates` reads `extracted_text`, returns a JSON list of {date, description, kind} and renders them with checkboxes; "Create selected" makes Tasks/CalendarEvents on the matter.
  4. Natural-language search: `/ai/search?q=` turns a question into structured filters (matter/contact/invoice/time/task) using the model, runs the queries, shows results; falls back to plain substring search across matters/contacts/documents when the model is unavailable, so the page always works.
  When `LLMUnavailable`, every feature shows a calm explanation and, where possible, the fallback. Tests use a monkeypatched `complete`.
- CRM pipeline: `/intake/pipeline` kanban (columns new, contacted, consult_scheduled, proposal, won, lost) built with plain HTML and a small vanilla JS drag/drop that POSTs `/intake/<id>/stage`; lead detail gains value, assigned user, next follow-up date, lost reason; converting a lead sets stage won; declining sets lost. Follow-up sequences `/intake/sequences` (CRUD, steps as day offset + subject + body with `{{ name }}`/`{{ firm_name }}`/`{{ matter_type }}` merge), "Start sequence" on a lead; `python -m app.cli sequences` sends the due step when `Firm.sequences_auto_send` is on, otherwise creates a Gmail-style draft record: store it as a Message channel=email direction=out status="draft" and list drafts at `/intake/drafts` with a Send button. Default is drafts (off), and the settings toggle copy must say sends happen only if the firm turns it on. Booking link: settings field `booking_url` does not exist in the schema; instead read `BOOKING_URL` env (add to config additive) and show it on the public intake thank-you page and in sequence merge as `{{ booking_url }}`.

## Report back
As in CONVENTIONS.md. Also list every env variable you introduced.
